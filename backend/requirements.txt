# Versions épinglées ; `pip-audit` (job `audit` de la CI) bloque sur toute
# vulnérabilité connue. Django 5.2 est une version LTS (corrigée jusqu'en
# avril 2028) ; DRF 3.17 corrige les avis portés sur 3.15.
Django==6.1.1
djangorestframework==3.18.1
django-filter==26.1
django-cors-headers==4.9.0
psycopg[binary]==3.3.6
gunicorn==26.2.0
django-storages[s3]==1.14.6
openpyxl==3.1.5
reportlab==5.0.1
apscheduler==3.11.3
# Analyse XML durcie (openpyxl et reportlab s'en servent si elle est présente).
defusedxml==0.7.1

# Double authentification (TOTP) et QR d'enrôlement
pyotp==2.10.0
qrcode[pil]==8.2
# Export Word
python-docx==1.2.0
# Métriques pour Prometheus / Grafana
django-prometheus==2.5.0
# Contrat d'API : schéma OpenAPI généré depuis les vues et sérialiseurs
# (``manage.py spectacular``), dont le frontend tire ses types.
drf-spectacular==0.30.0
# Sérialise les traces d'exception entre processus : sans lui, un test en
# échec sous ``manage.py test --parallel`` fait avorter tout le run.
tblib==3.2.2
# Surveillance des erreurs (config/surveillance.py). Sans SENTRY_DSN, la
# bibliothèque est présente mais rien ne s'installe et rien ne sort.
sentry-sdk==2.69.2
# Cache principal (config/settings.py, core/cache.py). Le client seul :
# le serveur est un conteneur de la pile. Sans REDIS_URL, rien ne s'en sert
# et la base de données reprend le rôle.
redis==8.1.0
